import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TAI-Farmos-IonicReact',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
